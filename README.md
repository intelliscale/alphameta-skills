# AlphaMeta Releases

## Get started

1. Install AlphaMeta:

    **MacOS (Recommended):**
    ```bash
    curl -fsSL https://alphameta.app/install.sh | bash
    ```

2. Start the server, run `alphameta start`.
